%li.foursquare.image-post
  %a{href: tip.url}
    .title.venue
      %h3
        = image_tag "foursquarefavicon.png"
        = tip.name
  .caption
    .text= raw(tip.text)
    .address= raw(tip.address)
    .time= pretty_time(tip.post_time)
    = render "feed_items/like", feed_item: tip
